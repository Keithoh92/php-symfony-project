{% extends 'base.html.twig' %}

{% block title %}UserBids{% endblock %}

{% block body %}
    <h1>UserBids</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_bid.id }}</td>
            </tr>
            <tr>
                <th>Item</th>
                <td>{{ user_bid.item }}</td>
            </tr>
            <tr>
                <th>Mybid</th>
                <td>{{ user_bid.mybid }}</td>
            </tr>
            <tr>
                <th>Currentbid</th>
                <td>{{ user_bid.currentbid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_bids_index') }}">back to list</a>

    <a href="{{ path('user_bids_edit', {'id': user_bid.id}) }}">edit</a>

    {{ include('user_bids/_delete_form.html.twig') }}
{% endblock %}
